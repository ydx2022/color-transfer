# 设计决策记录（DESIGN_DECISIONS）

> 记录「为什么这么定」，与《术语规范_GLOSSARY.md v1.2》配套。数值一律带单位；纯颜色型不出现"符号"一词。

## 技术债登记

| # | 债务 | 位置 | 影响 | 计划 |
|---|---|---|---|---|
| D1 | `ModulationScheme.cellPx` 未改名为 `symCellPx` | `web/src/shared/types.ts` | 语义歧义（v1.2 铁律 2 要求 `symCellPx`/`colCellPx` 分离） | 暂不改。改名会波及 `core/channelSim.ts`、`viz/frameRender.ts`、`viz/analyze.ts` 等**符号型遗留模块**，并打断 27 项单测。字段已就地加注释标注"遗留字段，语义为 v1.2 的 symCellPx，符号型专用"。待符号型模块归档后统一改名。 |
| D2 | 仿真未建模色度子采样 4:2:0 | `web/src/core/channelSim.ts` | 纯颜色型实际抗串扰能力被高估（色彩分辨率仅亮度 1/4） | 已知局限，不在本轮建模。改由真机对比序列（T1–T8）实测判定。 |
| D3 | 未建模卷帘快门 / 反光 / 眩光 | — | 真机格错误率可能高于仿真 | 已知局限。 |
| D4 | 校准格仅限于标准纯色 | `core/encoder.ts` `CELL_META_PILOT` | 无法同时校准亮度、色偏与位置 | 后话清单：可考虑已知图案 / 灰阶校准格。 |

## 已定案决策

| # | 决策 | 依据 | 状态 |
|---|---|---|---|
| A1 | `σ_PSF` 单位 = **屏幕像素** | `calibration/analyze_calibration.py:12/395/1085` + `calibration.json` 的 `psf.sigma_screen_px` | 已定案（任务 9 第 0 步） |
| A2 | 空间模糊模型 = 解析盒到盒权重（超采样高斯卷积的连续极限） | 取代 `1/(1+r²)`（对符号型过于乐观约 5 个数量级）；纯颜色型此前**完全没有**邻格串扰项 | 已落地（任务 9→10） |
| A3 | 三档改为**纯颜色型**（`symbolBits = 0`） | v12：净吞吐全面胜出（3.0× / 2.56× / 5.67×）；符号型 M1 在 web_auto 整文件成功率 0.0000 | 已定案，M1 推翻 |
| A4 | 最优 `colCellPx` **低于** `d_rec`（0.60~0.62×） | v13 细扫 13680 组：降色数拉大色间距，`colorBits ÷ colCellPx²` 的平方增益超过位宽损失 | **推翻 v12「= d_rec」结论**（任务 11 裁定采纳） |
| A5 | 校准格一律**与数据格同尺寸** | C 组：`web_auto` 下 1×1 / 2×2 校准格使成功率跌破 99.9% | 已定案 |
| A6 | 产品端逻辑网格按 `colCellPx` 反推，铺满全屏 | 旧 `GRIDS` 96×54 实测 `cellPx=20`，密度仅理论值 42% | 已落地 |
| A7 | 实测页为真实密度小块（约 640×360 屏幕像素），非全屏 | 便于手机对准构图；全屏满铺的是产品端 `/#/` | 已落地，UI 明示 |
